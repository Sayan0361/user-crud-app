<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= title %></title>
  <script src="https://cdn.tailwindcss.com"></script>

  <!-- Google Fonts: Poppins -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet" />
  <style>
    body {
      font-family: 'Poppins', sans-serif;
    }
  </style>
</head>
<body class="bg-gradient-to-br from-gray-900 via-zinc-900 to-gray-800 min-h-screen flex flex-col text-white">

  <!-- Top Navbar -->
  <div class="w-full flex justify-start px-6 py-4">
    <a href="/read" class="text-blue-400 text-sm font-medium hover:underline hover:text-blue-300 transition">
      &larr; View All Users
    </a>
  </div>

  <!-- Form Section -->
  <div class="flex-grow flex items-center justify-center px-4 pb-10">
    <div class="bg-white/5 backdrop-blur-md border border-white/10 rounded-3xl shadow-2xl p-8 w-full max-w-md transition duration-300 hover:shadow-blue-500/20">

      <form action="/create" method="POST" class="flex flex-col gap-5">
        <h1 class="text-4xl font-extrabold text-center mb-2 text-blue-400">Create New User</h1>
        <p class="text-sm text-center text-gray-400 mb-4">Fill out the form to add a new user</p>

        <input
          class="bg-white/5 border border-gray-600 placeholder-gray-400 text-white rounded-lg px-5 py-3 focus:outline-none focus:ring-2 focus:ring-blue-500 transition"
          type="text"
          placeholder="Full Name"
          name="name"
          required
        />

        <input
          class="bg-white/5 border border-gray-600 placeholder-gray-400 text-white rounded-lg px-5 py-3 focus:outline-none focus:ring-2 focus:ring-blue-500 transition"
          type="email"
          placeholder="Email Address"
          name="email"
          required
        />

        <input
          class="bg-white/5 border border-gray-600 placeholder-gray-400 text-white rounded-lg px-5 py-3 focus:outline-none focus:ring-2 focus:ring-blue-500 transition"
          type="url"
          placeholder="Profile Image URL"
          name="imageurl"
          required
        />

        <button
          class="bg-blue-600 hover:bg-blue-700 active:scale-95 transform transition-all duration-200 ease-in-out text-white font-bold py-3 rounded-xl shadow-md hover:shadow-blue-500/30"
          type="submit"
        >
            Create User
        </button>
      </form>
    </div>
  </div>

</body>
</html>
