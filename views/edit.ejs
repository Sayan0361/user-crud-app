<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= title %></title>
  <script src="https://cdn.tailwindcss.com"></script>

  <!-- Google Fonts: Poppins -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet" />
  <style>
    body {
      font-family: 'Poppins', sans-serif;
    }
  </style>
</head>
<body class="bg-gradient-to-br from-gray-900 via-zinc-900 to-gray-800 min-h-screen flex flex-col">

  <!-- Top Navbar / Link -->
  <div class="w-full flex justify-start px-6 py-4">
    <a href="/read" class="text-blue-400 text-sm font-medium hover:underline hover:text-blue-300 transition">
      &larr; View All Users
    </a>
  </div>

  <!-- Form Section -->
  <div class="flex-grow flex items-center justify-center px-4 pb-10">
    <div class="bg-white/5 backdrop-blur-lg border border-white/10 rounded-2xl p-8 w-full max-w-md shadow-xl hover:shadow-blue-500/20 transition duration-300">
      <form action="/update/<%= user._id %>" method="POST" class="flex flex-col gap-6">
        <h1 class="text-3xl font-extrabold text-center text-white mb-2">Update User</h1>
        
        <!-- Full Name -->
        <input
          type="text"
          name="name" 
          value="<%= user.name %>"
          placeholder="Full Name"
          required
          class="bg-zinc-800/40 border border-gray-600 text-white placeholder-gray-400 rounded-lg px-5 py-3 focus:outline-none focus:ring-2 focus:ring-blue-500 transition"
        />

        <!-- Email -->
        <input
          type="email"
          name="email" 
          value="<%= user.email %>"
          placeholder="Email Address"
          required
          class="bg-zinc-800/40 border border-gray-600 text-white placeholder-gray-400 rounded-lg px-5 py-3 focus:outline-none focus:ring-2 focus:ring-blue-500 transition"
        />

        <!-- Profile Image URL -->
        <input
          type="url"
          name="imageurl" 
          value="<%= user.imageurl %>"
          placeholder="Profile Image URL"
          required
          class="bg-zinc-800/40 border border-gray-600 text-white placeholder-gray-400 rounded-lg px-5 py-3 focus:outline-none focus:ring-2 focus:ring-blue-500 transition"
        />

        <!-- Submit Button -->
        <button
          type="submit"
          class="bg-green-600 hover:bg-green-700 text-white font-semibold py-3 rounded-lg transition duration-300 shadow-md hover:shadow-green-500/30"
        >
            Update User
        </button>
      </form>
    </div>
  </div>

</body>
</html>
