<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= title %></title>
  <link rel="stylesheet" href="/stylesheets/style.css" />
  <script src="https://cdn.tailwindcss.com"></script>

  <!-- Google Fonts: Poppins -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet" />
  <style>
    body {
      font-family: 'Poppins', sans-serif;
    }
  </style>
</head>
<body class="bg-gradient-to-br from-zinc-900 to-zinc-800 min-h-screen flex flex-col">

  <!-- Top Navbar / Link -->
  <div class="w-full flex justify-start px-6 py-4">
    <a href="/read" class="text-blue-400 text-sm hover:underline hover:text-blue-300 transition">
      &rarr; View Users
    </a>
  </div>

  <!-- Form Section -->
  <div class="flex-grow flex items-center justify-center px-4 pb-10">
    <div class="backdrop-blur-lg bg-zinc-800/10 border border-gray-600 rounded-2xl p-8 w-full max-w-md shadow-lg hover:shadow-2xl transition duration-300">
      <form action="/update/<%= user._id %>" method="POST" class="flex flex-col gap-5">
        <h1 class="text-3xl font-bold text-white text-center">Update User</h1>
        
        <input
          class="bg-zinc-900/40 border border-gray-600 text-white placeholder-gray-400 rounded-lg px-5 py-3 focus:outline-none focus:ring-2 focus:ring-blue-500 transition"
          type="text"
          placeholder="Full Name"
          name="name" 
          value="<%= user.name %>"
          required
        />

        <input
          class="bg-zinc-900/40 border border-gray-600 text-white placeholder-gray-400 rounded-lg px-5 py-3 focus:outline-none focus:ring-2 focus:ring-blue-500 transition"
          type="email"
          placeholder="Email Address"
          name="email" 
          value="<%= user.email %>"
          required
        />

        <input
          class="bg-zinc-900/40 border border-gray-600 text-white placeholder-gray-400 rounded-lg px-5 py-3 focus:outline-none focus:ring-2 focus:ring-blue-500 transition"
          type="url"
          placeholder="Profile Image URL"
          name="imageurl" 
          value="<%= user.imageurl %>"
          required
        />

        <button
          class="bg-green-600 hover:bg-green-700 text-white font-semibold py-3 rounded-lg transition duration-300"
          type="submit"
        >
          Update User
        </button>
      </form>
    </div>
  </div>

</body>
</html>
